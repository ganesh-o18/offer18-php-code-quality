#!/usr/bin/env php

<?php

$code = '<?php $hello = 1; ?>';

$tokens = token_get_all($code);

foreach ($tokens as $token) {
  if (is_array($token)) {
    echo token_name($token[0]) . ' ' . $token[1];
  }
}
// var_dump($tokens);